# CLOAK Backend - Environment Configuration Example
# Copy this file to .env.local, .env.dev, or .env.prod based on your environment
# DO NOT commit sensitive values to git

# ============================================================================
# LOCAL DEVELOPMENT (.env.local)
# ============================================================================
# Database: postgres://postgres:postgres@localhost:5432/cloak_db?sslmode=disable
# JWT_SECRET: dev-jwt-super-secret-local-only-12345
# HMAC_SECRET: dev-hmac-super-secret-local-only-12345
# LOG_LEVEL: debug

# ============================================================================
# DEVELOPMENT / STAGING (.env.dev)
# ============================================================================
# Database: postgres://dev_user:dev_password@dev-db.example.com:5432/cloak_dev?sslmode=require
# JWT_SECRET: Generate: openssl rand -base64 32
# HMAC_SECRET: Generate: openssl rand -base64 32
# LOG_LEVEL: info

# ============================================================================
# PRODUCTION (.env.prod)
# ============================================================================
# Database: postgres://prod_user:SECURE_PASSWORD@prod-db.example.com:5432/cloak_prod?sslmode=require
# JWT_SECRET: Generate: openssl rand -base64 32 (MUST be strong)
# HMAC_SECRET: Generate: openssl rand -base64 32 (MUST be strong)
# LOG_LEVEL: warn

# ============================================================================
# COMMON CONFIGURATION
# ============================================================================

# Server Configuration
PORT=8080
ENVIRONMENT=local

# Database Configuration (PostgreSQL)
# Format: postgres://[user]:[password]@[host]:[port]/[database]?sslmode=[disable|require]
DATABASE_URL=postgres://postgres:postgres@localhost:5432/cloak_db?sslmode=disable

# Security Keys
# Generate strong keys: openssl rand -base64 32
# Minimum 32 characters recommended
JWT_SECRET=your-jwt-secret-minimum-32-characters-long
HMAC_SECRET=your-hmac-secret-minimum-32-characters-long

# API Configuration
API_TIMEOUT=30s

# Logging (debug, info, warn, error)
LOG_LEVEL=debug

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000
